# DESIGN.md — طراحی SoulScreen

> **قانون طلایی این فایل:** به **طراحی** (ظاهر، رنگ‌ها، تم، فونت‌ها، فاصله‌ها، سبک کامپوننت‌ها) اپ **به هیچ وجه نباید دست زده شود.**
> تنها چیزی که مجاز به تغییر است — و باید فعالانه بهبود پیدا کند — **ریسپانسیو بودن** (رفتار چیدمان در اندازه‌ها/عرض‌های مختلف پنجره) است، بدون آنکه ظاهر بصری یا تم را تغییر دهد.

> ## ⚠️ تاکید حیاتی: اپ باید در **تمامی سایزها** کاملاً ریسپانسیو باشد
>
> این موضوع برای صاحب پروژه **بسیار مهم** است و یک الزام درجه‌یک محسوب می‌شود، نه یک «کار خوب است اگر وقت شد».
>
> - اپ باید از کوچک‌ترین اندازه‌ی مجاز پنجره (`MinWidth`/`MinHeight` فعلی در `MainWindow.xaml`) تا فول‌اسکرین روی هر مانیتوری (کوچک، معمولی، آلترا‌واید، ۴K) **بدون شکستگی چیدمان، هم‌پوشانی عناصر (overlap)، بریده‌شدن متن یا سرریز خارج از کادر** کار کند.
> - این الزام فقط پنجره‌ی اصلی نیست: **هر پنجره و هر پنل** — پنجره‌ی اصلی، پنل تنظیمات، گالری Captures، مینی‌پلیر، پالت فرمان، پاپ‌آپ‌ها و دیالوگ‌ها — باید در تمام اندازه‌های ممکن درست رندر شوند.
> - **هر بار که یک UI جدید اضافه یا یک UI موجود تغییر می‌کند، ریسپانسیو بودنش در چند اندازه‌ی نماینده (خیلی باریک نزدیک `MinWidth`، متوسط، پهن، فول‌اسکرین) باید بررسی/تست شود** — نه فقط در اندازه‌ی پیش‌فرض توسعه.
> - نبودِ ریسپانسیو بودن کامل یک **باگ** است، حتی اگر ظاهر در اندازه‌ی پیش‌فرض درست به نظر برسد.
> - وقتی شک داری که یک بخش در سایزهای غیرمعمول (خیلی کوچک یا خیلی بزرگ) چطور رفتار می‌کند، فرض را بر خراب بودن بگذار و بررسی/اصلاحش کن؛ منتظر گزارش کاربر نمان.

---

## 1. خلاصه اپ

**SoulScreen** یک اپلیکیشن ویندوزی (WPF, .NET 8, C#) برای نمایش آینه‌ای صفحه‌ی آیفون (و اندروید) روی ویندوز ۱۱ است (AirPlay / adb). رابط کاربری آن با الهام از زبان طراحی macOS/Apple ساخته شده: آرام، خنثی، تیره‌گرا با یک رنگ تاکیدی (accent) قابل انتخاب.

فایل مرجع تم: `src/SoulScreen.App/Theme.xaml`
منطق تم/رنگ تاکیدی: `src/SoulScreen.App/ThemeManager.cs`, `src/SoulScreen.App/Logic/AccentPalette.cs`
چیدمان اصلی پنجره: `src/SoulScreen.App/MainWindow.xaml` + پارشیال‌های `MainWindow.*.cs`
چارچوب پنجره‌ی سفارشی (caption buttons، درگ، ریسایز): `src/SoulScreen.App/WindowChrome.cs`

---

## 2. زبان طراحی (Design Language)

- **پالت خنثی و آرام، به سبک macOS:** تصویر آینه‌شده‌ی گوشی تنها چیزی است که باید چشم را جذب کند؛ هر سطح دیگر یک درجه از خاکستری است.
- **دو تم:** روشن (Light) و تیره (Dark) — از تنظیمات ویندوز پیروی می‌کند یا دستی قفل می‌شود.
- **هشت رنگ تاکیدی (Accent)** دقیقاً مطابق رنگ‌های سیستمی اپل: Blue (پیش‌فرض)، Purple، Pink، Red، Orange، Yellow، Green، Graphite. تعریف در `AccentPalette.cs`.
- **تایپوگرافی:**
  - آیکون‌ها: `Segoe Fluent Icons` (با فال‌بک `Segoe MDL2 Assets`)
  - متن: `Segoe UI Variable Text` (فال‌بک `Segoe UI`)
  - عناوین بزرگ: `Segoe UI Variable Display`
  - مقادیر عددی/متریک: `Cascadia Mono` (فال‌بک `Consolas`)
- **گوشه‌های گرد (Corner radius)** ثابت و هماهنگ در سراسر اپ: دکمه‌های آیکونی ۷px، دکمه‌های اصلی ۸px، کارت‌ها و پاپ‌آپ‌ها ۹–۱۴px، سوییچ‌ها کاملاً بیضی (pill).
- **سوییچ‌ها (Toggle)** به شکل pill اپل‌گونه با انیمیشن لغزش دسته (`Switch` style در `Theme.xaml`)، نه چک‌باکس مربعی ویندوزی.
- **کنترل سگمنتی (Segmented control)** برای انتخاب‌های ۲ تا ۴ گزینه‌ای (تم، Fit، تأخیر و…) به‌جای دراپ‌داون، تا همه‌ی گزینه‌ها هم‌زمان دیده شوند.
- **چارچوب پنجره‌ی سفارشی:** بدون تایتل‌بار استاندارد ویندوز؛ دکمه‌های caption (minimize/maximize/close) با اندازه‌ی ۴۴×۴۶ (متریک ویندوز ۱۱) خودِ اپ رسم می‌کند.
- **تصویر گوشی (Video surface) هرگز رنگ‌آمیزی نمی‌شود:** پس‌زمینه‌ی ویدیو همیشه مشکی مطلق (`VideoBackground`) است و برندهای HUD/overlay روی تصویر از پالت جدا و ثابتی استفاده می‌کنند که به تم بستگی ندارد.
- **موشن/انیمیشن:** گذارها کوتاه (`QuickDuration` = ۱۲۰ms)، با easing نرم؛ از تنظیم «کاهش موشن» ویندوز یا کلید دستی در Settings پیروی می‌کند (`Motion.cs`, `Logic/MotionPreferences.cs`, `Logic/UiMotion.cs`) — این رفتار بخشی از طراحی است و نباید حذف یا تغییر مفهومی شود.

---

## 3. ساختار صفحات و اجزای اصلی

| بخش | فایل(ها) |
| --- | --- |
| پنجره‌ی اصلی / تصویر گوشی | `MainWindow.xaml`, `MainWindow.Video.cs`, `Rendering/VideoSurface.cs` |
| نوار ابزار بالا (کنترل‌بار) | `MainWindow.ControlBar.cs`, `MainWindow.ControlBarPlacement.cs` |
| پالت فرمان (`Ctrl+K`) | `MainWindow.Commands.cs` |
| پنل تنظیمات (`Ctrl+,`) با سایدبار | `MainWindow.SettingsNav.cs` |
| گالری Captures (`Ctrl+G`) | `MainWindow.Captures.cs` |
| Mini player | `MainWindow.MiniPlayer.cs`, `Logic/MiniPlayerGeometry.cs` |
| Markup (قلم/هایلایتر/لیزر) | `MainWindow.Markup.cs` |
| فوکوس مود | `MainWindow.Focus.cs` |
| لاگ فعالیت (`Ctrl+L`) | `MainWindow.Log.cs` |
| آیکون Tray | `TrayIcon.cs` |
| کنترل‌های سفارشی | `Controls/SettingRow.cs`, `Controls/TileHost.xaml(.cs)` |

---

## 4. قوانین سخت‌گیرانه — این‌ها **ممنوع** است

اگر تسکی خواسته شد که یکی از موارد زیر را عوض کند، **اجرا نکن** و به کاربر اطلاع بده که این تغییر طراحی است، نه ریسپانسیو بودن:

1. **تغییر هیچ رنگی** در `Theme.xaml` یا `AccentPalette.cs` (کدهای هگز، برندهای `SolidColorBrush`، گرادیان‌ها).
2. **اضافه/حذف/تغییرِ رنگ تاکیدی (accent)** یا منطق مشتق‌سازی سایه‌های آن.
3. **تغییر فونت‌ها** (`IconFont`, `TextFont`, `DisplayFont`, `MonoFont`) یا اندازه/وزن تایپوگرافی تعریف‌شده در استایل‌های `Heading`, `Body`, `Caption` و…
4. **تغییر `CornerRadius`، ضخامت border، سایه (DropShadowEffect)، یا فاصله‌گذاری (Padding/Margin) استاندارد** کامپوننت‌های موجود (دکمه‌ها، سوییچ، کارت‌ها، پاپ‌آپ‌ها).
5. **تغییر ظاهر/رفتار بصریِ** سوییچ pill، Segmented control، دکمه‌های caption، آیکون‌های Segoe Fluent، یا استایل کلی HUD روی تصویر.
6. **بازنویسی یا حذف `ThemeManager.cs`، `WindowChrome.cs`، یا منطق تم روشن/تیره.**
7. **معرفی رنگ، فونت، یا افکت جدیدی که در پالت فعلی نیست** — حتی به بهانه‌ی «بهبود ظاهر».
8. **تغییر مفهومی سیستم موشن/انیمیشن** (مدت‌زمان، easing، یا این‌که چه چیزی متحرک می‌شود) — این بخشی از هویت طراحی است.

این فایل‌ها **قفل طراحی** محسوب می‌شوند و فقط برای رفع باگِ واقعی (نه بهبود سلیقه‌ای) باید لمس شوند، و آن هم با تأیید صریح کاربر:

```
src/SoulScreen.App/Theme.xaml
src/SoulScreen.App/App.xaml
src/SoulScreen.App/ThemeManager.cs
src/SoulScreen.App/Logic/AccentPalette.cs
src/SoulScreen.App/WindowChrome.cs
```

---

## 5. چیزی که **مجاز و الزامی** است — ریسپانسیو بودن

هدف: در **تمامی** اندازه‌های پنجره — از حداقل مجاز تا فول‌اسکرین، روی هر رزولوشن و هر نسبت تصویری — همان زبان طراحی دقیقاً عوض نمی‌شود، اما **چیدمان** (layout) باید همیشه منطقی، بدون overlap، بدون بریده‌شدن محتوا و بدون اسکرول‌بار غیرمنتظره بماند. این یک هدف مستمر است، نه یک‌بار انجام‌شده: با هر ویژگی‌ی جدید UI، ریسپانسیو بودنش دوباره تضمین شود. این یعنی می‌توان (و باید در صورت نیاز):

- **نقاط شکست (breakpoint)** برای عرض پنجره اضافه/تنظیم کرد — مثل الگوی موجود در `MainWindow.SettingsNav.cs` (`SettingsSidebarBreakpoint = 700`) که زیر آن سایدبار تنظیمات پنهان می‌شود.
- **تعداد ستون‌های گالری Captures** را بر اساس عرض موجود تغییر داد (`MainWindow.Captures.cs` → `NarrowestForTwoColumns`, `MinCaptureTileWidth`).
- **حالت فشرده (Compact) نوار ابزار** را گسترش داد: کم/زیاد شدن برچسب دکمه‌ها هنگام کمبود جا (`MainWindow.ControlBarPlacement.cs` → `ApplyCompactMode`, `CompactControlBar`).
- **چیدمان مینی‌پلیر** را برای اندازه‌های مختلف صفحه تنظیم کرد (`Logic/MiniPlayerGeometry.cs`) — تا وقتی شکل ظاهری (رنگ، گوشه‌ها، دکمه‌ها) عوض نشود.
- **قرارگیری/wrap متن‌ها، دکمه‌ها یا ردیف‌های تنظیمات** را برای پنجره‌ی باریک بازچینی کرد (مثلاً استک‌شدن یک ردیف افقی به عمودی)، تا وقتی استایل هر عنصر (فونت، رنگ، گوشه) دست‌نخورده بماند.
- **`MinWidth` / `MinHeight`** پنجره یا حداقل اندازه‌ی اجزا را برای جلوگیری از شکستگی چیدمان در صفحات کوچک تغییر داد.
- منطق `SizeChanged` جدید برای هر ناحیه‌ای که هنوز واکنش‌گرا نیست اضافه کرد، به شرطی که فقط اندازه/چیدمان (Grid columns, Visibility, Orientation, Width) را عوض کند — نه رنگ، فونت یا گوشه.
- **هر بخشی از UI که فعلاً در سایزهای کوچک یا بزرگ خراب می‌شود را شناسایی و اصلاح کرد**، حتی اگر تسک اصلی چیز دیگری بوده — رفتار ریسپانسیو یک استثنای همیشه-مجاز روی «بدون drive-by refactor» در `AGENTS.md` است.

### قاعده‌ی تشخیص سریع
> اگر تغییر فقط عدد **اندازه/موقعیت/تعداد ستون/نمایش‌دادن یا مخفی‌کردن بر اساس عرض** را عوض می‌کند → مجاز (ریسپانسیو).
> اگر تغییر **رنگ، فونت، گوشه‌ی ثابت، سایه، یا شمایل بصری کامپوننت** را عوض می‌کند → ممنوع (طراحی/تم).

---

## 6. پروتکل وسواسی: بررسی ریسپانسیو هنگام افزودن هر قابلیت جدید

هر بار که یک **قابلیت جدید** (پنل، کارت تنظیمات، دیالوگ، نوار، تایل، badge، منو، هر عنصر بصری تازه) به اپ اضافه می‌شود، این بررسی باید **دقیق‌ترین و با‌وسواس‌ترین شکل ممکن** انجام شود — سرسری رد شدن از این مرحله قابل قبول نیست. هدف این نیست که «در حالت پیش‌فرض خوب به نظر برسد»؛ هدف این است که در **هیچ اندازه‌ای** خراب نباشد.

### 6.1 مرحله‌ی طراحیِ چیدمان (پیش از نوشتن کد)

- مشخص کن این عنصر جدید در Grid/Panel چه رفتاری در برابر تغییر عرض/ارتفاع دارد: `*`، `Auto`، `MinWidth`، `MaxWidth`، `Wrap`، `TextTrimming` کجا لازم است؟
- برای هر متنِ پویا (نام دستگاه، برچسب، مقدار) از ابتدا `TextTrimming="CharacterEllipsis"` یا رفتار wrap مناسب در نظر بگیر — هیچ متنی نباید در پنجره‌ی باریک از کادر بیرون بزند.
- اگر عنصر در نوار ابزار/کنترل‌بار قرار می‌گیرد، رفتارش در حالت Compact (`ApplyCompactMode`) را از همان ابتدا تعریف کن: برچسب‌متنی حذف می‌شود؟ خودش هم مخفی می‌شود؟ در الویت پایین‌تری قرار می‌گیرد؟
- اگر یک breakpoint جدید لازم است، آن را به‌صورت ثابتِ نام‌دار در `Logic/*.cs` تعریف کن (مثل الگوی `SettingsSidebarBreakpoint`)، نه عدد جادویی داخل XAML یا code-behind.

### 6.2 مرحله‌ی پیاده‌سازی

- منطق تصمیم‌گیری چیدمان (چند ستون؟ کدام‌ها مخفی شوند؟ Orientation عمودی یا افقی؟) در یک تابع خالص و قابل تست در `Logic/*.cs` بنویس — دقیقاً الگوی جدول بخش ۷ («الگوهای موجود ریسپانسیو»)، پایین‌تر.
- به رویداد `SizeChanged` عنصر/کانتینر مربوطه وصل کن، نه فقط به `Window.SizeChanged` — یک پنل تودرتو ممکن است زودتر یا دیرتر از پنجره تنگ شود.
- مطمئن شو تغییر اندازه هیچ برشی (Clip) ناخواسته، هیچ اسکرول‌بار افقی غیرمنتظره، و هیچ هم‌پوشانی با عناصر مجاور ایجاد نمی‌کند.

### 6.3 مرحله‌ی بررسی وسواسی (اجباری، پیش از اعلام «تمام شد»)

قابلیت جدید باید دست‌کم در این سایزهای مشخص، **تک‌تک**، بررسی/شبیه‌سازی شود — نه فقط اندازه‌ی توسعه:

| # | سایز | چرا |
| --- | --- | --- |
| 1 | دقیقاً روی `MinWidth`×`MinHeight` پنجره (کوچک‌ترین حالت مجاز) | جایی که اولین شکستگی رخ می‌دهد |
| 2 | یک پله بالاتر از حداقل (مثلاً ~۴۰۰–۵۰۰px عرض) | ناحیه‌ی مرزی بین Compact و حالت عادی |
| 3 | اندازه‌ی پیش‌فرض اپ (۹۲۰×۶۸۰) | حالت رایج‌ترین کاربر |
| 4 | یک پنجره‌ی پهنِ معمولی (مثلاً ۱۴۰۰+ عرض) | جایی که فضای خالی زیاد می‌شود |
| 5 | Maximized / فول‌اسکرین روی صفحه‌ی معمولی (۱۹۲۰×۱۰۸۰) | رایج‌ترین حالت واقعی استفاده |
| 6 | یک صفحه‌ی آلترا‌واید یا ۴K اگر منطقاً ممکن است | اطمینان از عدم کشیدگی/خالی ماندن نامتناسب |
| 7 | تغییر اندازه‌ی **زنده** (درگ‌کردن گوشه‌ی پنجره پیوسته از کوچک به بزرگ و برعکس) | رفتار گذار بین breakpointها، نه فقط حالت‌های ثابت |

برای هر ردیف بالا، این سؤال‌ها را با دقت جواب بده — نه با حدس:

- آیا متنی بریده یا truncate ناخواسته شده؟
- آیا دو عنصر روی هم افتاده‌اند (overlap)؟
- آیا چیزی از مرز پنجره/کارت/پنل بیرون زده؟
- آیا فاصله‌گذاری (padding/margin) نامتوازن یا بیش‌ازحد فشرده به نظر می‌رسد؟
- آیا در گذار بین دو breakpoint یک پرش ناگهانی و زشت دیده می‌شود؟
- آیا رفتار در RTL/چپ‌به‌راست هم (اگر مربوط است) درست است؟

اگر جواب هر کدام «نامشخص» یا «نه» است، قابلیت **کامل نیست** — برگرد و اصلاح کن، بعد دوباره کل جدول بالا را تکرار کن.

### 6.4 اصل بدون استثنا

> **هیچ قابلیتی «تمام‌شده» اعلام نمی‌شود مگر اینکه در تمام ۷ سناریوی بالا بررسی و تأیید شده باشد.**
> این حتی برای تغییرات به‌ظاهر کوچک (یک دکمه‌ی جدید، یک ردیف تنظیمات تازه) هم صدق می‌کند — یک عنصر کوچک هم می‌تواند در سایز اشتباه چیدمان کل نوار یا کارت را بشکند.
> اگر بررسی دستی/بصری روی هاردور ممکن نیست، حداقل منطق چیدمان را با تست یونیت روی مقادیر عرض متعدد (`Theory`/`InlineData` با چند عرض مختلف) پوشش بده — دقیقاً همان‌طور که `Logic/MiniPlayerGeometry.cs` و مشابهش تست شده‌اند.

---

## 7. الگوهای موجود ریسپانسیو (برای الگوبرداری)

| الگو | فایل | نکته |
| --- | --- | --- |
| مخفی/نمایش سایدبار تنظیمات بر اساس عرض | `MainWindow.SettingsNav.cs` | ثابت `SettingsSidebarBreakpoint` |
| تغییر تعداد ستون گالری | `MainWindow.Captures.cs` | `UpdateCaptureTileWidth()` |
| فشرده‌سازی نوار ابزار (مخفی‌کردن برچسب متنی دکمه‌ها) | `MainWindow.ControlBarPlacement.cs` | `ApplyCompactMode()` |
| اندازه‌ی مینی‌پلیر متناسب با صفحه | `Logic/MiniPlayerGeometry.cs` | تابع خالص، بدون WPF، قابل تست |
| چیدمان مجدد لاگ فعالیت | `MainWindow.Log.cs` | `UpdateLogHeaderLayout()` |

هرگونه گسترش ریسپانسیو جدید باید همین الگو را دنبال کند: منطقِ خالصِ محاسبه‌ی چیدمان در `Logic/*.cs` (بدون وابستگی به WPF، قابل تست یونیت)، و اتصال آن به رویداد `SizeChanged` در پارشیال کلاس مربوطه.

---

## 8. جمع‌بندی برای هر تغییر

پیش از هر Pull Request یا commit مربوط به UI، این چک‌لیست را رد کن:

- [ ] هیچ رنگ/هگزی در `Theme.xaml` یا `AccentPalette.cs` تغییر نکرده.
- [ ] هیچ فونتی عوض نشده.
- [ ] هیچ `CornerRadius`، سایه، یا فاصله‌ی ثابتِ یک کامپوننتِ موجود دست‌نخورده مانده.
- [ ] تنها چیزی که تغییر کرده منطق اندازه/چیدمان/breakpoint است.
- [ ] **تمام ۷ سناریوی بخش ۶.۳ (حداقل سایز، مرز Compact، پیش‌فرض، پهن، فول‌اسکرین، آلترا‌واید/۴K، تغییر اندازه‌ی زنده) برای این قابلیت به‌صورت وسواسی و تک‌تک بررسی شده — نه فقط چشمی و سریع.**
- [ ] هیچ overlap، بریدگی متن، سرریز از کادر، یا اسکرول‌بار غیرمنتظره در هیچ‌کدام از آن سناریوها دیده نشده.
- [ ] اگر پنل/پاپ‌آپ/دیالوگ جدیدی اضافه شده، همان مجموعه سناریوها برای آن هم جداگانه تکرار شده.
- [ ] در صورت امکان، منطق چیدمان با تست یونیت روی چند عرض مختلف پوشش داده شده (طبق بخش ۶.۴).
- [ ] هیچ ریسپانسیو ناقصی «برای بعد» رها نشده — این یک الزام درجه‌یک پروژه است، نه یک nice-to-have.
