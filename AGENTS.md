# AGENTS.md — راهنمای عامل‌های کدنویسی برای SoulScreen

این فایل راهنمای هر ایجنت هوش مصنوعی (Claude Code، Codex، OpenCode و مشابه) است که روی این ریپازیتوری کار می‌کند. پیش از هر تغییری این فایل را بخوان، بعد `DESIGN.md` را — که مرز طراحی را مشخص می‌کند — و طبق هر دو عمل کن.

---

## 1. مهم‌ترین قانون: طراحی دست‌نخورده می‌ماند

جزئیات کامل در **`DESIGN.md`** آمده؛ خلاصه‌اش:

- **به طراحی اپ (رنگ‌ها، تم، فونت‌ها، گوشه‌ها، سایه‌ها، سبک کامپوننت‌ها) به هیچ وجه دست نزن.**
- **ریسپانسیو بودن (رفتار چیدمان در عرض‌ها/اندازه‌های مختلف پنجره) می‌تواند تغییر و بهبود پیدا کند.**
- **به تم (`Theme.xaml`, `ThemeManager.cs`, `AccentPalette.cs`, `App.xaml`, `WindowChrome.cs`) دست نزن**, مگر برای رفع یک باگ مشخص و با تأیید صریح کاربر.
- پیش از هر PR/commit مرتبط با UI، چک‌لیست بخش ۷ فایل `DESIGN.md` را رعایت کن.

اگر یک درخواست کاربر مستقیماً خواستار تغییر رنگ/فونت/تم/گوشه/سایه است، **متوقف شو و از کاربر تأیید صریح بگیر** — این تغییر خارج از حیطه‌ی معمول تسک‌هاست و باید آگاهانه درخواست شود.

---

## 2. شناخت پروژه

SoulScreen یک گیرنده‌ی آینه‌سازی صفحه‌ی آیفون/اندروید روی ویندوز ۱۱ است (WPF, .NET 8, C#, x64). جزئیات کامل معماری در `README.md` (بخش‌های Status و Layout).

```
src/
  SoulScreen.Core/       قراردادهای مستقل از ترنسپورت: IMirrorSource، نمونه‌های مدیا، لاگ
  SoulScreen.AirPlay/    گیرنده‌ی بی‌سیم (mDNS، RTSP، Pairing، FairPlay، Streams)
  SoulScreen.Usb/        ترنسپورت USB آیفون (ناتمام)
  SoulScreen.Android/    آینه‌سازی اندروید روی adb
  SoulScreen.Media/      دیکد، پیسینگ، پخش، ضبط
  SoulScreen.App/        پوسته‌ی WPF: چارچوب، تصویر، تنظیمات، لاگ، Tray، متریک‌ها
  SoulScreen.Cli/        گیرنده‌ی headless و ابزارهای پروتکل
tests/SoulScreen.Tests/  تست‌های پروتکل، رمزنگاری و فرمت وایر
tools/                   اسکریپت‌های بیلد و دیاگنوستیک
```

`SoulScreen.AirPlay` هیچ وابستگی به UI ندارد و می‌تواند مستقل embed شود.

---

## 3. ساخت و تست

```powershell
dotnet build
dotnet test
python tools/check-plist-interop.py    # بعد از تست‌ها
```

- `dotnet build` باید بدون هشدار جدید تمیز اجرا شود.
- `dotnet test` باید همه‌ی تست‌های موجود + تست‌های جدید را پاس کند.
- برای اجرای خود اپ: `dotnet run --project src/SoulScreen.Cli -- serve --dump capture` یا `RUN.bat`.
- اسکریپت‌های `tools/*.ps1` زیر Windows PowerShell 5.1 اجرا می‌شوند، نه `pwsh` جدا.

---

## 4. قراردادهای کد

- **Namespace به‌صورت file-scoped.**
- کلاس‌ها جایی که منطقی است `sealed`.
- primary constructor فقط جایی که پروژه از قبل استفاده کرده.
- برِیس‌استایل مطابق `.editorconfig` (در صورت وجود) و سبک فایل‌های مجاور.
- **بدون drive-by refactor.** فقط چیزی را تغییر بده که تسک ایجاب می‌کند؛ سبک فایل موجود را حفظ کن.
- **بدون وابستگی (NuGet) جدید**، مگر با یک کامنت صریح `// Approved:` در توضیح PR و تأیید کاربر.
- منطق خالص و قابل تست (بدون وابستگی به WPF) در `Logic/*.cs` نگه داشته می‌شود — نمونه: `Logic/MiniPlayerGeometry.cs`, `Logic/DisplayLayout.cs`. الگوی ریسپانسیو جدید هم باید همین ساختار را دنبال کند.
- کلاس‌های پنجره‌ی اصلی به‌صورت partial تقسیم شده‌اند (`MainWindow.*.cs`) — این سبک را حفظ کن، فایل تازه اضافه نکن مگر لازم باشد.

---

## 5. مرجع کد را دقیق بده

- در گزارش‌ها و توضیح تغییرات، به کد به‌صورت `path:line` ارجاع بده، نه چسباندن کل فایل.
- پیش از وصله زدن، جستجو (grep/glob) را واقعاً اجرا کن و نتیجه را گزارش بده — حدس نزن.

---

## 6. ایمنی و دامنه

- هرگز secrets را نخوان، لاگ نکن یا commit نکن (`.env`, کلیدهای واقعی در `appsettings*.json` و مشابه).
- هرگز `git commit` یا `git push` نکن مگر کاربر صریحاً بخواهد.
- بعد از حدود ۳ تلاش ناموفق روی یک فایل، متوقف شو و از کاربر بپرس؛ در حلقه گیر نکن.
- تغییرات خارج از scope تسک (فایل‌های نامرتبط، رفکتور سلیقه‌ای) را انجام نده.

---

## 7. قبل از اتمام هر تسک UI

1. `DESIGN.md` بخش ۷ (چک‌لیست) را رعایت کن.
2. `dotnet build` و در صورت لزوم `dotnet test` را اجرا کن.
3. اگر تغییر مربوط به چیدمان/ریسپانسیو بود، رفتار را در حداقل عرض پنجره (`MinWidth`) و در حالت فول‌اسکرین هر دو تصور/بررسی کن.
4. اگر تغییر خواسته‌شده در واقع طراحی/تم بود نه ریسپانسیو بودن، پیش از اجرا از کاربر تأیید بگیر.
